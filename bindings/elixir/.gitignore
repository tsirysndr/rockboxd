/_build/
/deps/
/doc/
/priv/*.so
erl_crash.dump
*.ez

# Precompiled-NIF checksum file — generated at release time by
# `mix elixir_make.checksum`, shipped in the Hex package, not committed.
# elixir_make >= 0.8 emits a fixed `checksum.exs`; keep the legacy glob too.
/checksum.exs
/checksum-*.exs
/*.tar
